import { ExistedvaluePipe } from './existedvalue.pipe';

describe('ExistedvaluePipe', () => {
  it('create an instance', () => {
    const pipe = new ExistedvaluePipe();
    expect(pipe).toBeTruthy();
  });
});
